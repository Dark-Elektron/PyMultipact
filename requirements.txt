# Dependencies are declared in pyproject.toml -- this file is a convenience
# pointer for `pip install -r requirements.txt`.
#
# Extras: [post] vtk/numba post-processing, [dev] tests + notebooks,
#         [docs] sphinx documentation build.
-e .[post,dev,docs]
